import { StyleSheet } from 'react-native';

const theme = {
    light: {
        item: {}
    },
    dark: {
        item: {
            color: 'white'
        }
    }
};

const styles = StyleSheet.create({

})
export {
    styles, theme
}